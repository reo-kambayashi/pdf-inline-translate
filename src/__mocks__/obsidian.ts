// Minimal stub for the 'obsidian' package used in unit tests.
// Only the APIs referenced by the tested source files need to be here.
export class Notice {
    constructor(public message: string) {}
}
